cmake_minimum_required(VERSION 3.17.5)
project(xxhash LANGUAGES C VERSION 0.8.3)

set(DEFAULT ${BUILD_SHARED_LIBS})
option(BUILD_SHARED "build a shared library" ${DEFAULT})
if(DEFAULT)
    set(DEFAULT OFF)
else()
    set(DEFAULT ON)
endif()
option(BUILD_STATIC "build a static library" ${DEFAULT})

foreach(VARIANT SHARED STATIC)
    add_library(xxhash_${VARIANT} ${VARIANT})
    set_target_properties(xxhash_${VARIANT} PROPERTIES OUTPUT_NAME xxhash SOVERSION 0)
    target_sources(xxhash_${VARIANT} PRIVATE xxhash.c)
    if(CMAKE_BUILD_TYPE STREQUAL "Debug")
        target_compile_definitions(xxhash_${VARIANT} PRIVATE XXH_NO_INLINE_HINTS)
    endif()
    install(TARGETS xxhash_${VARIANT})
endforeach()

include(GNUInstallDirs)

function(generate_pkgconfig_file)
    set(PREFIX ${CMAKE_INSTALL_PREFIX})
    set(EXECPREFIX ${CMAKE_INSTALL_FULL_LIBEXECDIR})
    set(INCLUDEDIR ${CMAKE_INSTALL_FULL_INCLUDEDIR})
    set(LIBDIR ${CMAKE_INSTALL_FULL_LIBDIR})
    set(VERSION ${PROJECT_VERSION})
    configure_file(libxxhash.pc.in libxxhash.pc @ONLY)
endfunction()
generate_pkgconfig_file()

install(FILES xxh3.h xxhash.h DESTINATION ${CMAKE_INSTALL_INCLUDEDIR})
install(FILES ${CMAKE_CURRENT_BINARY_DIR}/libxxhash.pc DESTINATION ${CMAKE_INSTALL_LIBDIR}/pkgconfig)
