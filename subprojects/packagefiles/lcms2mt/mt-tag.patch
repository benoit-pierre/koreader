diff --git a/meson.build b/meson.build
--- a/meson.build
+++ b/meson.build
@@ -1,4 +1,4 @@
-project('Little-CMS', 'c',
+project('Little-CMS-MT', 'c',
   version: '2.14',
   meson_version: '>=0.48.0',
 )
@@ -37,8 +37,8 @@ lcms2_srcs = [
 ]
 
 install_headers([
-  'include/lcms2.h',
-  'include/lcms2_plugin.h'
+  'include/lcms2mt.h',
+  'include/lcms2mt_plugin.h'
 ])
 
 inc_dirs = include_directories('include')
@@ -63,17 +63,17 @@ endif
 
 if host_machine.system() == 'windows'
   win = import('windows')
-  lcms2_srcs += win.compile_resources('Projects/VC2019/lcms2.rc')
+  lcms2_srcs += win.compile_resources('Projects/VC2019/lcms2mt.rc')
   cargs += '-DCMS_DLL_BUILD=1'
 endif
 
-liblcms2_lib = library('lcms2', lcms2_srcs,
+liblcms2_lib = library('lcms2mt', lcms2_srcs,
   include_directories : inc_dirs,
   gnu_symbol_visibility: 'hidden',
   dependencies: deps,
   c_args: cargs,
   version: library_version,
-  vs_module_defs: 'src/lcms2.def',
+  vs_module_defs: 'src/lcms2mt.def',
   install: true,
 )
 
