diff --git a/meson.build b/meson.build
--- a/meson.build
+++ b/meson.build
@@ -1223,7 +1223,7 @@ sdl2main_dep = declare_dependency(
 
 deps = [core_deps, extra_deps]
 
-sdl2 = library('sdl2',
+sdl2 = library('SDL2-2.0',
     all_sources,
     include_directories : core_inc,
     c_args : core_args,
@@ -1232,6 +1232,7 @@ sdl2 = library('sdl2',
     override_options : ['c_std=none'],
     install : do_install,
     gnu_symbol_visibility: 'hidden',
+    version: '0',
 )
 
 if platform_is_windows and get_option('with_main')
